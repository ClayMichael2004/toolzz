# Toolzz AI — Frontend Client

The frontend interface for Toolzz AI, built with React, Vite, and a minimal black design system.

## Available Scripts

- `npm run dev` — Starts local development server on port 5173
- `npm run build` — Builds production bundle to `dist/`
- `npm run preview` — Previews production build locally
